import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  root: {
    padding: "10px 20px",
    margin: "0px",
  },
  filter: {
    minWidth: "120px",
  },
  createButton: {
    marginLeft: "auto"
  }
}));

export default useStyles;
